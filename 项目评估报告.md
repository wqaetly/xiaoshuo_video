# 小说转视频生成系统 - 项目评估报告

## 一、项目概述

本项目是一个基于混合方案的小说转视频自动化生成框架，将小说文本自动转换为带配音和字幕的视频。采用本地服务+远端API的混合架构。

---

## 二、现有运行架构

```
┌───────────────────────────────────────────────────────────────────┐
│                        WebUI (Gradio)                             │
│                     src/webui.py - 7860端口                        │
└───────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌───────────────────────────────────────────────────────────────────┐
│                   Pipeline Controller                             │
│              src/pipeline/controller.py                           │
│    状态管理: PipelineState (7个阶段流程控制)                        │
└───────────────────────────────────────────────────────────────────┘
                                 │
         ┌───────────────────────┼───────────────────────┐
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   LLM模块       │    │   Image模块     │    │   TTS模块       │
│  (本地Ollama)   │    │  (本地ComfyUI)  │    │ (本地CosyVoice) │
│  qwen2.5:14b    │    │     SDXL        │    │   语音合成      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Video模块 (远端API)                         │
│              即梦AI / 可灵AI - 图生视频                           │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Compose模块 (FFmpeg)                         │
│            视频合成 + 音频混合 + 字幕叠加                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、模块详细分析

### 3.1 入口模块 (src/main.py)

**实现功能:**
- CLI命令行接口: `create`, `run`, `ui`, `status`
- 项目创建和管理
- 断点续传支持

**完成度:** ★★★★☆ (80%)

**改进方向:**
- 缺少参数校验和错误提示优化
- 缺少批量处理命令
- 建议添加 `export` 命令导出项目

---

### 3.2 WebUI模块 (src/webui.py)

**实现功能:**
- 6个Tab页: 项目管理、分镜编辑、角色管理、生成控制、预览、设置
- 实时进度展示
- 场景编辑和保存

**完成度:** ★★★☆☆ (65%)

**改进方向:**
- [ ] 缺少WebSocket实时日志推送，当前使用轮询
- [ ] 预览功能不完整，章节预览未实现
- [ ] 缺少任务队列可视化
- [ ] 角色编辑Tab未完整绑定事件
- [ ] 建议添加拖拽调整场景顺序
- [ ] 建议添加批量操作(删除、重新生成)

---

### 3.3 Pipeline模块 (src/pipeline/)

**实现功能:**
- 7阶段流程: INIT → ANALYZE → CHARACTER_DESIGN → GENERATE_IMAGES → GENERATE_AUDIO → GENERATE_VIDEO → COMPOSE
- JSON状态持久化，支持断点续传
- 进度回调机制
- VRAM自动清理

**完成度:** ★★★★☆ (85%)

**改进方向:**
- [ ] 缺少并行任务调度(当前串行执行)
- [ ] 错误重试机制较简单，建议使用指数退避
- [ ] 建议添加任务优先级队列
- [ ] 建议支持跳过失败场景继续执行

---

### 3.4 LLM模块 (src/llm/)

**实现功能:**
- `OllamaClient`: 健康检查、聊天、流式输出、嵌入生成
- `StoryboardGenerator`: 章节分割、分镜生成、JSON解析修复
- `CharacterExtractor`: 角色提取、SD提示词生成

**完成度:** ★★★★☆ (80%)

**改进方向:**
- [ ] 章节分割逻辑较简单，对非标准格式支持差
- [ ] JSON解析失败率高，建议使用结构化输出(JSON Mode)
- [ ] 缺少提示词模板文件化(当前硬编码)
- [ ] 建议添加上下文窗口管理(长文本分块)
- [ ] 建议支持多LLM后端(OpenAI兼容API)

---

### 3.5 Image模块 (src/image/)

**实现功能:**
- `ComfyUIClient`: 完整API封装(上传、队列、等待、下载)
- `SceneGenerator`: 场景图生成、提示词构建、多风格预设
- `CharacterDesigner`: 多角度角色立绘生成

**完成度:** ★★★☆☆ (70%)

**改进方向:**
- [ ] 工作流硬编码，缺少自定义工作流加载
- [ ] 角色一致性差，建议集成IP-Adapter/InstantID
- [ ] 缺少ControlNet姿态控制
- [ ] 缺少LoRA动态加载
- [ ] 建议添加图像质量检测(BRISQUE/NIQE)
- [ ] 建议支持图像修复重绘(局部不满意重新生成)

---

### 3.6 Video模块 (src/video/)

**实现功能:**
- 抽象基类 `VideoAPIClient` + 工厂模式
- `JimengClient`: 即梦AI完整API(上传、生成、状态查询、闲时折扣)
- `KlingClient`: 可灵AI API(视频生成、延长)

**完成度:** ★★★☆☆ (60%)

**改进方向:**
- [ ] **API未经实际测试**，URL和参数需根据官方文档调整
- [ ] 缺少API错误码处理和账户余额检查
- [ ] 缺少并发请求限制(Rate Limiting)
- [ ] 建议添加成本预估功能
- [ ] 建议支持更多视频API(Runway, Pika等)
- [ ] 建议添加视频质量评估

---

### 3.7 TTS模块 (src/tts/)

**实现功能:**
- `CosyVoiceClient`: 基础TTS、音色克隆、场景音频合成
- `VoiceManager`: 角色音色分配、自动匹配

**完成度:** ★★★☆☆ (65%)

**改进方向:**
- [ ] **CosyVoice API格式需验证**，当前为假设实现
- [ ] 音色预设较少，建议支持自定义音色导入
- [ ] 缺少SSML支持(语调、停顿控制)
- [ ] 缺少多语言支持
- [ ] 建议添加Edge-TTS作为备选方案
- [ ] 音频段直接拼接，缺少时间轴对齐

---

### 3.8 Compose模块 (src/compose/)

**实现功能:**
- `VideoComposer`: FFmpeg封装、视频拼接、BGM混合
- `SubtitleGenerator`: SRT/ASS字幕生成
- `AudioMixer`: 多音轨混合、音量标准化

**完成度:** ★★★★☆ (75%)

**改进方向:**
- [ ] 缺少转场效果(淡入淡出、交叉溶解)
- [ ] 字幕位置和样式定制化不足
- [ ] 缺少视频分辨率/帧率自动统一
- [ ] 建议支持多种输出格式(MP4/WebM/MOV)
- [ ] 建议添加视频压缩选项

---

### 3.9 Utils工具模块 (src/utils/)

**实现功能:**
- `config.py`: Pydantic配置模型、环境变量支持、热重载
- `logger.py`: Loguru日志系统、文件轮转
- `file_utils.py`: JSON/YAML/文本文件操作

**完成度:** ★★★★★ (90%)

**改进方向:**
- [ ] 配置校验可以更严格
- [ ] 建议添加配置变更监听

---

## 四、整体评估总结

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | ★★★★☆ | 模块化清晰，流程解耦，便于扩展 |
| 代码质量 | ★★★★☆ | 类型注解完整，日志规范，异常处理合理 |
| 功能完整度 | ★★★☆☆ | 核心流程可用，部分功能未完全实现 |
| 可用性 | ★★★☆☆ | 依赖服务较多，部署复杂，需大量配置 |
| 稳定性 | ★★☆☆☆ | 外部API未经生产验证，错误处理需加强 |

---

## 五、优先改进建议

### 高优先级
1. **验证外部API**: 即梦/可灵/CosyVoice API需实际测试并修正
2. **增强错误处理**: 添加完整的错误码处理和重试机制
3. **并行任务调度**: 图像/音频生成可以并行执行
4. **角色一致性**: 集成IP-Adapter保持角色形象统一

### 中优先级
5. **提示词模板化**: 将LLM提示词外置为可配置文件
6. **WebUI增强**: 实时日志、任务队列、批量操作
7. **视频转场**: 添加淡入淡出等基础转场效果
8. **Edge-TTS备选**: 降低本地部署难度

### 低优先级
9. **多LLM支持**: 支持OpenAI兼容API
10. **成本监控**: 添加API调用成本统计
11. **导出功能**: 支持导出项目为独立包

---

## 六、技术债务

- `jimeng.py` / `kling.py`: API URL和参数为示例，需根据官方文档修正
- `cosyvoice_client.py`: API格式为假设实现
- 分镜JSON解析失败率高，需要更健壮的解析策略
- ComfyUI工作流硬编码，应支持外部JSON加载

---

## 七、模块完成度汇总

| 模块 | 完成度 | 关键问题 |
|------|--------|----------|
| Pipeline | 85% | 缺少并行调度 |
| LLM | 80% | JSON解析失败率高 |
| Utils | 90% | 基本完善 |
| Main入口 | 80% | 缺少批量命令 |
| Compose | 75% | 缺少转场效果 |
| Image | 70% | 角色一致性差 |
| WebUI | 65% | 多功能未完整实现 |
| TTS | 65% | API未验证 |
| Video | 60% | API为示例代码 |

**项目整体完成度: 约 73%**
